export const metadata = {
  title: "Home | FoundIt",
  description: "Welcome to FoundIt, the premier lost and found platform for LSPU students. Discover recent lost items, report findings, and connect with the community.",
};

export default function HomeLayout({ children }) {
  return <>{children}</>;
}
