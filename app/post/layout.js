export const metadata = {
  title: "Post Item | FoundIt",
  description: "Report a lost or found item at LSPU. Provide details like location, category, and an image to help the community.",
};

export default function PostLayout({ children }) {
  return <>{children}</>;
}
