export const metadata = {
  title: "Items | FoundIt",
  description: "Browse the comprehensive catalog of lost and found items at LSPU. Search by category, status, and location to find what you're looking for.",
};

export default function ItemsLayout({ children }) {
  return <>{children}</>;
}
