export const metadata = {
  title: "Admin Dashboard | FoundIt",
  description: "FoundIt administration dashboard for moderating items, managing users, reviewing AI logs, and handling reports.",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
